{
  "name": "lingua-greeter-client",
  "dependencies": {
    "google-protobuf": "^3.21.2",
    "grpc-web": "^1.4.2"
  },
  "devDependencies": {
    "browserify": "^16.2.2",
    "rimraf": "^3.0.2",
    "webpack": "^4.16.5",
    "webpack-cli": "^3.1.0"
  },
  "scripts": {
    "clean": "rimraf dist",
    "build": "rimraf dist && npx webpack greeter_client.js"
  }
}